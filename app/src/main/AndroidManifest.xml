<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:name=".CurioNextApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CurioNext"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- Main Dashboard Activity -->
        <activity
            android:name=".features.dashboard.DashboardActivity"
            android:exported="true"
            android:theme="@style/Theme.CurioNext.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Feature Activities -->
        <activity
            android:name=".features.location.LocationTrackingActivity"
            android:parentActivityName=".features.dashboard.DashboardActivity" />
        <activity
            android:name=".features.interests.InterestAnalysisActivity"
            android:parentActivityName=".features.dashboard.DashboardActivity" />
        <activity
            android:name=".features.notifications.NotificationActivity"
            android:parentActivityName=".features.dashboard.DashboardActivity" />
        <activity
            android:name=".features.preferences.PreferenceAnalysisActivity"
            android:parentActivityName=".features.dashboard.DashboardActivity" />
        <activity
            android:name=".features.profile.ChildProfileActivity"
            android:parentActivityName=".features.dashboard.DashboardActivity" />
        <activity
            android:name=".features.safety.SafetySettingsActivity"
            android:parentActivityName=".features.dashboard.DashboardActivity" />

        <!-- Background Services -->
        <service
            android:name=".core.service.LocationTrackingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

        <service
            android:name=".core.service.AudioProcessingService"
            android:enabled="true"
            android:exported="false" />

        <!-- Firebase Messaging (if using FCM) -->
        <service
            android:name=".core.service.NotificationService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_GOOGLE_MAPS_API_KEY" />

    </application>

</manifest>